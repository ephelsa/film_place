// ignore_for_file: prefer_const_constructors
import 'package:actor_repository/actor_repository.dart';
import 'package:test/test.dart';

void main() {
  group('ActorRepository', () {
    test('can be instantiated', () {
      expect(ActorRepository(), isNotNull);
    });
  });
}
