name: hive_local_storage
description: Local database for caching and more for Flutter
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.1.0
  hive_flutter: ^1.1.0
  rxdart: ^0.27.3

dev_dependencies:
  build_runner: ^2.1.10
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.2
  very_good_analysis: ^2.4.0
