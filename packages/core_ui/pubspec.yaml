name: core_ui
description: A Flutter package with the core ui elements for Film Place
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  lottie: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^2.4.0

flutter:
  assets:
    - packages/core_ui/assets/empty-box.json
    - packages/core_ui/assets/oops-animation.json
